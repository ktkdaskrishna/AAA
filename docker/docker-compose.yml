version: "3.9"
services:
  gateway:
    build:
      context: ..
      dockerfile: docker/gateway.Dockerfile
    env_file:
      - ../.env.example
    volumes:
      - axiom_audit:/var/log/axiom
    restart: unless-stopped
volumes:
  axiom_audit:
